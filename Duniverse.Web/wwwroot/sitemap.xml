<?xml version="1.0" encoding="UTF-8"?>
<!-- Just the home page for now. The way this site deploys, every other route gets served
     with a 404 status, so search engines can't index them no matter what this file claims.
     Listing them would just fill Search Console with missing-page reports. Once those routes
     get served as real files, they belong in here too.

     Google ignores changefreq and priority, so I left them out. It does read lastmod, which
     is the date the page content last changed, not the date of the last deploy. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://www.duniverse.org/</loc>
    <lastmod>2026-07-20</lastmod>
  </url>
</urlset>
